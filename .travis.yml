language: python
sudo: false
cache: pip
services:
    - postgresql
    - mysql
env:
  global:
    - LD_PRELOAD=/lib/x86_64-linux-gnu/libSegFault.so
    - SEGFAULT_SIGNALS=all
    - TRHOME=~travis
  matrix:
    - TOXENV=check
    - TOXENV=docs
matrix:
  include:
    - python: '2.7'
      env:
        - TOXENV=py27-sqlite,report,codecov
    - python: '2.7'
      env:
        - TOXENV=py27-sqlite_file,report,codecov
    - python: '2.7'
      env:
        - TOXENV=py27-postgresql,report,codecov
    - python: '2.7'
      env:
        - TOXENV=py27-mysql,report,codecov
    - python: '3.4'
      env:
        - TOXENV=py34-sqlite,report,codecov
    - python: '3.4'
      env:
        - TOXENV=py34-sqlite_file,report,codecov
    - python: '3.4'
      env:
        - TOXENV=py34-postgresql,report,codecov
    - python: '3.4'
      env:
        - TOXENV=py34-mysql,report,codecov
    - python: '3.5'
      env:
        - TOXENV=py35-sqlite,report,codecov
    - python: '3.5'
      env:
        - TOXENV=py35-sqlite_file,report,codecov
    - python: '3.5'
      env:
        - TOXENV=py35-postgresql,report,codecov
    - python: '3.5'
      env:
        - TOXENV=py35-mysql,report,codecov
    - python: '3.6'
      env:
        - TOXENV=py36-sqlite,report,codecov
    - python: '3.6'
      env:
        - TOXENV=py36-sqlite_file,report,codecov
    - python: '3.6'
      env:
        - TOXENV=py36-postgresql,report,codecov
    - python: '3.6'
      env:
        - TOXENV=py36-mysql,report,codecov
before_install:
  - python --version
  - uname -a
  - lsb_release -a
install:
  - pip install tox
  - virtualenv --version
  - easy_install --version
  - pip --version
  - tox --version
before_script:
    - mysql -e 'CREATE DATABASE IF NOT EXISTS test;'
    - mysql -e "CREATE USER 'scott' IDENTIFIED BY 'tiger'; GRANT ALL ON test.* TO 'scott'@'localhost';"
    - psql -U postgres -c "CREATE USER scott WITH PASSWORD 'tiger'; create database test; grant all privileges on database test to scott;"
script:
  - tox -v
after_failure:
  - more .tox/log/* | cat
  - more .tox/*/log/* | cat
notifications:
  email:
    on_success: never
    on_failure: always

